version: '3'

services:
  ci-build:
    image: microsoft/aspnetcore-build:1.0-2.0
    volumes:
      - .:/src
    working_dir: /src
    command: /bin/bash -c "dotnet restore ./JAlgoArena-Judge-CSharp.sln && dotnet publish ./JAlgoArena-Judge-CSharp.sln -c Release -o ./obj/Docker/publish"
